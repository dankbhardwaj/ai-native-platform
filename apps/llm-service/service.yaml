apiVersion: v1
kind: Service
metadata:
  name: llm-service
  namespace: default
  labels:
    app: llm-service
spec:
  type: NodePort
  selector:
    app: llm-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 32083
